The simulation. Plain TypeScript: no React, no DOM, no canvas. Must be able to run in a Web Worker or Node.
One module per system, registered on the clock. State changes only through commands.
May import: data/.
