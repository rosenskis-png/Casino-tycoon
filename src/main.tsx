// Toolchain placeholder. Replaced by the real app shell in milestone M1.
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root")!).render(
  <p style={{ color: "#ffd36b", font: "16px system-ui", padding: 24 }}>Casino Tycoon: engine not built yet.</p>,
);
