// Results hold until they're shown (owner, M8.5): the sim settles a spin or a hand at once, but while its animation
// plays the top bar keeps the cash it had (less the bet), so nothing gives the result away early.
export const reveal = { cash: null as number | null };
